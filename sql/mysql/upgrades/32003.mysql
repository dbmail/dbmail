
BEGIN;

ALTER TABLE {PREFIX}messages ADD seq BIGINT not null default '0';
CREATE INDEX {PREFIX}messages_seq_index ON {PREFIX}messages(seq);

INSERT INTO {PREFIX}upgrade_steps (from_version, to_version, applied) values (32001, 32003, now());

COMMIT;

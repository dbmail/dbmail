
BEGIN;

CREATE TABLE {PREFIX}upgrade_steps (
  from_version int not null,
  to_version int not null,
  applied datetime not null,
  unique key (from_version, to_version)
);

INSERT INTO {PREFIX}upgrade_steps (from_version, to_version, applied) values (0, 32001, now());

COMMIT;


BEGIN;

ALTER TABLE {PREFIX}messages ADD COLUMN seq BIGINT NOT NULL default '0';

CREATE INDEX {PREFIX}messages_seq_index ON {PREFIX}messages(seq);

INSERT INTO {PREFIX}upgrade_steps (from_version, to_version) values (32001, 32003);

COMMIT;


BEGIN;

CREATE TABLE {PREFIX}upgrade_steps (
  from_version int not null,
  to_version int not null,
  applied TIMESTAMP WITHOUT TIME ZONE DEFAULT NOW()
);

CREATE UNIQUE INDEX {PREFIX}upgrade_steps_index_1 ON {PREFIX}upgrade_steps (from_version, to_version);
INSERT INTO {PREFIX}upgrade_steps (from_version, to_version) values (0, 32001);

COMMIT;

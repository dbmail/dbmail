$Id$

2004-01-19 (Roel Rozendaal):
FETCH MSGNUM (RFC822.HEADER) returns headers in wrong order.

2004-03-02 ():
IMAP APPEND should handle optional INTERNAL DATE and FLAGS

2004-03-08 (Ilja Booij):
IMAP ACLs seem to be off. -> Problem with Mozilla because of this?

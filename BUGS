$Id$

2004-01-19 (Roel Rozendaal):
FETCH MSGNUM (RFC822.HEADER) returns headers in wrong order.

2004-01-29 (Ilja Booij):
db_getmailbox seems to hang (last thing in log seems to indicate
that it's this functions that's hanging.


